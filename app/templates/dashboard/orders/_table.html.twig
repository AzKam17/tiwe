{% if orders|length > 0 %}
    <div class="card shadow-sm rounded">
        <div class="card-header fw-bold d-flex justify-content-between align-items-center">
            <span>Mes commandes</span>
            <span class="badge bg-primary rounded-pill">{{ orders|length }}</span>
        </div>
        <div class="card-body p-0">
            <table class="table mb-0 table-outer-border">
                <thead class="bg-light">
                <tr>
                    <th>ID</th>
                    <th class="text-end">Acheteur</th>
                    <th class="text-end">Montant</th>
                    <th class="text-end">Frais</th>
                    <th class="text-end">Total</th>
                    <th>Date</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for order in orders %}
                    <tr>
                        <td><strong>#{{ order.slug }}</strong></td>
                        <td class="text-end">
                            <i class="bi bi-person-fill me-1"></i>
                            {{ order.buyer.firstName ~ ' ' ~ order.buyer.lastName }}
                        </td>
                        <td class="text-end">{{ order.amount|number_format(2, ',', ' ') }} €</td>
                        <td class="text-end">{{ order.fees|number_format(2, ',', ' ') }} €</td>
                        <td class="text-end"><strong>{{ order.totalAmount|number_format(2, ',', ' ') }} €</strong></td>
                        <td>{{ order.createdAt|date('d/m/Y H:i') }}</td>
                        <td>
                            <a href="{{ path('app_auth_orders_details', {id: order.id}) }}" class="btn btn-sm btn-outline-primary align-items-center gap-1">
                                <i class="bi bi-eye"></i> Voir
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% else %}
    <div class="card text-center shadow-sm p-4 rounded">
        <div class="card-body">
            <i class="bi bi-cart-x fs-1 mb-3"></i>
            <h5 class="card-title">Aucune commande pour le moment</h5>
            <p class="card-text text-muted">
                Vous n'avez encore passé aucune commande. Explorez les offres et faites vos premiers achats !
            </p>
            <a href="#" class="btn btn-outline-primary d-flex align-items-center gap-1">
                <i class="bi bi-bag-plus"></i> Commencer à commander
            </a>
        </div>
    </div>
{% endif %}
