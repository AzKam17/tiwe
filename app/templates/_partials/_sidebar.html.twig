<nav class="nav flex-column pt-3">
    <a class="nav-link d-flex align-items-center mb-2 {% if app.request.attributes.get('_route') == 'app_dashboard_home' %}text-primary{% endif %}"
       href="{{ path('app_dashboard_home') }}" data-turbo-frame="main-content-frame">
        <i class="bi bi-box-seam me-2"></i> Accueil
    </a>

    <a class="nav-link d-flex align-items-center mb-2 {% if app.request.attributes.get('_route') == 'app_auth_products_home' %}text-primary{% endif %}"
       href="{{ path('app_auth_products_home') }}" data-turbo-frame="main-content-frame">
        <i class="bi bi-box-seam me-2"></i> Mes produits
    </a>
    {# Add more menu items here later #}

    <div class="pb-3">
        <a class="nav-link d-flex align-items-center text-primary" target="_top" href="{{ path('app_home') }}">
            <i class="bi bi-house-door me-2"></i> Retour au site
        </a>
    </div>
</nav>
